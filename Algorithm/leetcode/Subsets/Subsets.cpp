#include <iostream>
#include <vector>

using namespace std;

/*
题意：
给定一个没有重复数字的集合，返回所有可能的子集。
其中子集有以下要求：
1、子集中的元素必须以非降序排列。
2、子集不能重复。
思路：

*/
class Solution {
public:
	vector<vector<int>> subsets(vector<int>& nums) {

	}
};

int main(void)
{
	

	return 0;
}